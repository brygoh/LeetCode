class Solution:
    def calculateTax(self, brackets: List[List[int]], income: int) -> float:
        tax = prev = 0
        for upper, interest in brackets:
            if (income >= upper):
                tax += (upper - prev) * interest / 100
                prev = upper
            else:
                tax += (income - prev) * interest / 100
                return tax
        return tax